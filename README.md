# GitHub Achievements NFT Drop

## Commands

To run local blockchain instance use script

``` npm run launch-devnet ```

Compile Contract with

``` npm run compile-all ```

Deploy locally

``` npm run deploy-devnet ```
